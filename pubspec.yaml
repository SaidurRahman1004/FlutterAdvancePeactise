name: fluttert_test_code
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter
  # ICONS
  cupertino_icons: ^1.0.8
  # LOCAL STORAGE
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # STATE MANAGEMENT
  provider: ^6.1.2 # Updated to latest stable version
  # NETWORKING
  http: ^1.2.1 # Updated to latest stable version
  # FIREBASE (ALL UPDATED TO BE COMPATIBLE)
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.6.12
  firebase_messaging: ^15.0.2 # Dependent on other Firebase versions
  firebase_storage: ^12.0.2  # Dependent on other Firebase versions
  # GOOGLE SIGN IN (UPDATED)
  google_sign_in: ^6.2.1
  shared_preferences: ^2.5.3





dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1 # TypeAdapter জেনারেট করার জন্য
  build_runner: ^2.4.9


  flutter_lints: ^5.0.0


# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/google_logo.png

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
