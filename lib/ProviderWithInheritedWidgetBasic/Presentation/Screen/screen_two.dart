import 'package:flutter/material.dart';

class secondCounter extends StatelessWidget {
  const secondCounter({super.key, });


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Second Counter"),
      ),

    );
  }
}